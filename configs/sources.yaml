sources:
  sql:
    type: jdbc
    description: "PostgreSQL source database (users, products, orders, order_items)"
  api:
    type: http
    description: "FastAPI clickstream event source"
  excel:
    type: file
    path: excel
    file_pattern: "*.xlsx"
