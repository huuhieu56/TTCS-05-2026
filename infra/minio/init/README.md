# MinIO Init

Thư mục chứa script khởi tạo bucket/chính sách cho MinIO nếu cần.

- Bucket theo chuẩn zone: `raw`, `clean`, `serving`.
- Cấu hình endpoint và credentials nằm trong `infra/.env.example`.
