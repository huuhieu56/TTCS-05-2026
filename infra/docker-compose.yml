services:
  minio:
    image: minio/minio:RELEASE.2024-11-07T00-52-20Z
    container_name: ttcs-minio
    command: server /data --console-address ":9001"
    env_file:
      - .env
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5

  clickhouse:
    image: clickhouse/clickhouse-server:24.9
    container_name: ttcs-clickhouse
    env_file:
      - .env
    ports:
      - "8123:8123"
      - "9002:9000"
    volumes:
      - clickhouse_data:/var/lib/clickhouse
      - ./clickhouse/init:/docker-entrypoint-initdb.d
      - ./clickhouse/users.d:/etc/clickhouse-server/users.d
    healthcheck:
      test: ["CMD-SHELL", "clickhouse-client --query 'SELECT 1'"]
      interval: 10s
      timeout: 5s
      retries: 5

  source-db:
    image: postgres:16-alpine
    container_name: ttcs-source-db
    environment:
      POSTGRES_DB: ${SOURCE_PG_DB:-ecommerce}
      POSTGRES_USER: ${SOURCE_PG_USER:-source_user}
      POSTGRES_PASSWORD: ${SOURCE_PG_PASSWORD:-source_pass}
    ports:
      - "5432:5432"
    volumes:
      - source_pg_data:/var/lib/postgresql/data
      - ./source-db/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${SOURCE_PG_USER:-source_user} -d ${SOURCE_PG_DB:-ecommerce}"]
      interval: 10s
      timeout: 5s
      retries: 5

  source-api:
    build: ./source-api
    container_name: ttcs-source-api
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 10s
      timeout: 5s
      retries: 5

  spark:
    image: apache/spark:3.5.3
    container_name: ttcs-spark
    command: ["/bin/bash", "-lc", "sleep infinity"]
    env_file:
      - .env
    ports:
      - "4040:4040"
      - "8080:8080"
    # Mount only the directories the pipeline actually needs, not the full workspace.
    volumes:
      - ../pipelines:/workspace/pipelines
      - ../warehouse:/workspace/warehouse
      - ../configs:/workspace/configs
      - ../data_source:/workspace/data_source
    depends_on:
      minio:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
      source-db:
        condition: service_healthy
      source-api:
        condition: service_healthy

  streamlit:
    # Pre-install dependencies at image build time (see apps/streamlit/Dockerfile)
    # rather than at container startup to avoid slow, non-reproducible installs.
    image: python:3.11.10-slim
    container_name: ttcs-streamlit
    working_dir: /workspace
    command: sh -c "pip install --no-cache-dir -r requirements.txt && streamlit run app.py --server.port=8501 --server.address=0.0.0.0 --server.headless=true"
    env_file:
      - .env
    ports:
      - "8501:8501"
    # Mount only the Streamlit app directory, not the full workspace.
    volumes:
      - ../apps/streamlit:/workspace
    depends_on:
      clickhouse:
        condition: service_healthy

volumes:
  minio_data:
  clickhouse_data:
  source_pg_data:
