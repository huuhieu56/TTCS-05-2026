# Spark Config

Thư mục chứa cấu hình Spark cho môi trường local/dev.

- Ví dụ: `spark-defaults.conf`, `log4j2.properties`.
- Job Spark của pipeline được đặt tại `pipelines/transform.py` và `pipelines/transforms/`.
